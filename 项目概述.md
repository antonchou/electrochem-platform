# 项目概述（读懂这个项目，从这一篇开始）

> 所属课题：树莓派在电化学设备的应用开发 —— **溶液导电性相对比较实验平台**
> 版本基线：main @ `663e4a0`（2026-09-05 复核；原稿为 09-03 快照）｜ 详细技术细节见 `项目文档.md`，进度真相见根 `README.md`

---

## 0. 电梯陈述（30 秒版）

这是一个**能真正跑起来的电导率测量实验平台**：对导电池施加受控交流激励，同步采集电极**电压 U**、回路**电流 I** 与**温度 T**，软件链算出电导 G、电导率 κ(T)，再温度补偿到 κ25；浏览器上实时看波形与 I–V 特性、自动判稳给 QC 结论、可选化学公式拟合，并把每一帧原始量**不可篡改地**存进 SQLite，随时回看与导出。

目前硬件尚未接入，数据来自三套可切换的模拟源（Mock / 电压扫描仿真器 / echemdb CSV 回放）；但驱动层已按统一异步边界抽象，真实 ADC 到位时只需替换驱动，业务与前端不动。

---

## 1. 背景与要解决的问题

### 1.1 科学问题

比较不同溶液（不同浓度/种类）的**导电能力强弱**。要拿来做比较的量，不是“某个芯片报出来的数字”，而是可追溯、可复算、可标定的物理量：**25 ℃ 下的电导率 κ25**。

### 1.2 为什么不用“电导率模块直出 EC”

早期路线考虑 BA121S/CM2 这类现成电导率模块（直接输出 EC）。该路线已被明确废弃（`docs/电导率I-V测量链路与开发路线.md`）：

- 模块输出的是**结果**而不是原始量，无法溯源、无法复算、无法证明标定与温补是否正确；
- 量程、激励、温补模型被封装在黑盒里，无法作为实验证据。

现行路线是**自己测**：激励 → 采 U/I → 算 G → 乘 Kcell → 温补。每一步都有参数、有版本、有质量标志，可以被复核。

### 1.3 工程目标

| 目标 | 落地方式 |
|---|---|
| 实时可视化 | WebSocket 10Hz 推帧，浏览器画 V(t)/I(t)、I–V 散点 |
| 数据可信 | 原始帧 append-only（SQLite 触发器禁 UPDATE/DELETE）+ 分层存储 |
| 可复现 | 设备/激励/校准/温补参数随帧落库，`calibration_id` 溯源 |
| 可演示 | 无硬件也能完整演示（模拟源 + 纯浏览器模式） |
| 可交接 | 严格仓库约定、版本化配置、迁移框架、E2E 验收 |

---

## 2. 测量原理（整个项目的物理内核）

```
受控交流激励 → 导电池电极 → 电压通道 U + 电流通道 I → ADC
                                            ↓
        温度 T ──→  G(T) = I / U  ──→  κ(T) = Kcell × G(T)  ──→  κ25
                      [S]                    [μS/cm]             温补后
```

- **κ25 = κ(T) / [1 + α × (T − 25)]**（线性温补，仅在适用温区使用）
- **Kcell** 由标准液标定得到（当前模拟源为声明值，真实标定属后续阶段）
- **U ≤ 0 或数据非法时**：不硬算、不崩溃 —— 原始 U/I/T 照常落库（Raw 不可变），Derived 置空并打 `COMPUTE_INVALID` 标志

**数据分层（CONTRIBUTING §6 强制）**

| 层 | 内容 | 性质 |
|---|---|---|
| Raw | `voltage_raw_v / current_raw_a / temperature_raw_c` | **不可变**，任何校准/滤波/温补都不得覆盖 |
| Calibrated | `conductance_s / kappa_t_us_cm` | 应用校准与电池常数后的可重算结果 |
| Derived | `kappa_25_us_cm`、QC 代表值、拟合结果 | 温补/判稳/统计/拟合产物 |
| Trace | `seq_no / timestamp_utc / monotonic_ms / sensor_path_id / calibration_id` | 时序与溯源 |
| Configuration | 激励频率/幅值、`range_id`、`compensation_model` | 必须随实验版本化 |
| Quality | `quality_flags` | 饱和/开路/未校准/计算无效/落库丢帧等 |

---

## 3. 系统全貌

```
┌── 硬件（后续阶段）──────────────────────────────────────────────┐
│ 导电池/电极 · 交流激励源 · 电压/电流 AFE · ADC（候选 ADS1256）      │
│ DS18B20 温度 · ESP32-S3 / 树莓派时序控制  ← 当前由驱动层模拟替代     │
└───────────────┬─────────────────────────────────────────────────┘
                │  DeviceDriver 统一异步边界（read(elapsed) → DriverReading）
┌───────────────▼────────────── 后端 FastAPI :8000 ──────────────┐
│ 单一采集任务（10Hz）：driver.read → 计算链 → 落库 → 广播            │
│ ├ measurement.py  G→κ(T)→κ25        ├ analysis.py   化学公式拟合  │
│ ├ stability.py    判稳/QC            ├ storage.py    SQLite+迁移   │
│ ├ persistence.py  异步攒批落库        └ broadcast.py  有界队列广播   │
│ 驱动：MockDevice（默认）| SimulatorDriver | CsvPlaybackDriver       │
│ 存储：experiments / samples / raw_frames / calibration_records /    │
│       fit_results                                                  │
└───────┬──────────────────────────────────┬───────────────────────┘
        │ WS /ws/stream（帧与状态）          │ REST /api/...（控制/历史/拟合）
┌───────▼──────────────────────────────────▼─────── 前端 React 18 ─┐
│ services: WebSocketClient（重连+看门狗）| ApiClient | BrowserMock   │
│ hooks: useConnection / useExperiment / useRealtimeData / useIVAnalysis │
│ 页面：实时波形 → I–V 核心图 → 溶液比较 → 结果(含 QC) → 拟合 → 诊断 →   │
│       历史面板（列表/详情/静态曲线/CSV+JSON 导出）                    │
└──────────────────────────────────────────────────────────────────┘
```

目标机：Raspberry Pi OS 桌面版（实测 Debian 13 trixie / aarch64 / Python 3.13.5），生产由 FastAPI 在 `:8000` **同源托管**前端产物 + API + WS，Chromium Kiosk 全屏自启。

---

## 4. 一次实验的完整旅程

| 步骤（用户） | 背后发生什么 |
|---|---|
| 填「溶液/样品」「浓度 mmol/L」 | 浓度写入 samples，用于 Kohlrausch / 线性标定拟合 |
| 点「开始实验」 | 建实验+样品（同一事务）→ 写校准记录 → 内存态 running → 广播状态帧 |
| 看实时波形 | 10Hz 帧经 WS 到达 → 解析校验 → 写缓冲（上限 2 万点）→ 200ms 节流重绘 |
| 看 I–V 核心图 | 对最近数据做 OLS：跨度 ≥0.05V 且 R²≥0.95 才把斜率当电导 G，否则用各点 I/U 均值并给原因 |
| 点「停止」 | flush 落库 → 对最近 500 帧判稳写 QC（PASS/WARN/FAIL + 代表值）→ 实验置 stopped |
| 看结果区 | QC 徽标/代表值/中位数/原因 + 均值/最值/末值 + 导出 CSV |
| 点「开始拟合」 | 选 X 轴语义（时间/温度/浓度）→ 对应化学模型池 → R² 排序 + AICc/MAE/LOOCV/有效区间，可入库并写 `data/derived/` |
| 换溶液再测 | 「溶液比较」柱状图会话内累计（不读历史库）；换样品或 reset 会开新实验 |
| 点「历史实验」 | 列表 → 详情（样品表/静态曲线降采样/复用拟合）→ CSV 或 JSON 导出 |

**续跑语义**：停止后**同样品**再 start = 继续同一条实验记录（`resumed:true`），时间轴从暂停点续上并回灌历史帧；换样品或先 reset 才开新实验。

---

## 5. 关键设计取舍

| 取舍 | 为什么 |
|---|---|
| **单一后台采集任务** | 采集与落库跟连接数解耦：无浏览器也采、多浏览器不重复采 |
| **每客户端有界队列广播** | 慢客户端只会被丢旧帧（保最新），不会反过来压垮采集循环 |
| **异步攒批落库 + flush 屏障** | 200 条或 0.5s 批量写；stop 前保证“之前入队的都提交”后再置终态 |
| **落库失败降级而非中断** | 持久化 degraded 后拒收新帧、只广播一次告警、帧打 `PERSIST_DROPPED` 仍推送 —— 实时曲线不断，历史/导出会缺帧并被明确告知 |
| **append-only + 版本化迁移** | 触发器禁改删；`schema_migrations` 驱动 v1→v6，每步独立事务（源于一次 V2 迁移事故的复盘） |
| **驱动统一边界** | 未来 ADS1256 适配器在 `_build_driver()` 注册即可，业务层无分支 |
| **`ec` 保留为 κ25 兼容别名** | 旧前端/旧测试不受影响；新量走 U/I/T → G → κ(T) → κ25 |
| **拟合按 X 轴语义分池** | 时间轴不会给你 Arrhenius，浓度轴才给 Kohlrausch，避免“数学上能拟合、化学上无意义” |
| **判稳优先判漂移** | 先查硬质量标志，再看趋势斜率，再看变异系数（纯噪声与系统性漂移要区分） |
| **模拟/真实只差一个地址** | `VITE_WS_URL` / `VITE_API_BASE` 一改即可切真实后端，核心代码零改动 |

**关键参数**：采样 10Hz ｜ 前端缓冲上限 20 000 点 ｜ 曲线 200ms 节流 ｜ 断线看门狗 3s ｜ 落库攒批 ≤200 条/0.5s ｜ 判稳窗口 30 点（最少 10 点）｜ I–V 线性门槛 跨度 0.05V、R² 0.95。

---

## 6. 当前能力边界（重要：不要误判完成度）

**已具备**
- 完整 Web 实验闭环：实时流、控制、波形/I–V/比较/诊断、结果统计
- SQLite 存储（append-only）+ 迁移框架 + 历史查询 + CSV/JSON 导出
- 自动判稳 QC（PASS/WARN/FAIL + 原因 + 代表值）
- 化学公式拟合（time/temperature/concentration 三轴，含 AICc/MAE/残差/Wald CI/LOOCV/禁止外推）
- 三套模拟数据源 + 故障注入 + 调试接口
- Playwright E2E 验收 + CI（backend/frontend/e2e 三 job）

**尚未具备**（冻结 SRS 中已登记，非缺陷）
- 真实电压/电流采集链路（AFE/ADC 台架）与真实温度通道（DS18B20）
- `Kcell` 标准液标定 SOP 与真实校准记录
- 温补模型在真实温区的验证、滤波值分层显示
- PDF/Excel 报表（当前仅 CSV/JSON）
- 鉴权与访问控制（当前为开发/演示的开放模式）

---

## 7. 质量现状（2026-09-05 实测，main @ 663e4a0）

| 项目 | 结果 |
|---|---|
| 后端 `pytest` | **131 passed**（2 条第三方库弃用警告；09-03 基线为 125，其后 PR #18–#20 新增回归测试 +6） |
| 前端 `npm test` | **18/18 passed**（09-03 基线为 15，新增 `controlError.test.ts` +3） |
| 前端 `npm run typecheck` | 通过，0 错误 |
| E2E | 26 个用例（F01–F10、P02/P04、拟合、Phase7、截图）；P03 长跑需 `STABILITY_MINUTES` 显式开启，不进 CI |
| CI | push/PR 触发三 job（pytest / typecheck+单测 / Playwright chromium） |

已知技术债与 Bug 清单（含图表轴边界、长跑性能、续跑去重、落库降级自愈等）统一记录在 `项目文档.md` §12。
**2026-09-05 复核**：清单中 B10/B11/B12/P1-A/P1-B/P2-A/P2-B 共 **7 项已修复**（PR #18–#20 及更早提交），仍开放的高优先级项为 B1（电流换挡 Y 轴量纲混算）与 B2（缓冲封顶后全量重建），详见 `项目文档.md` §12 逐项标注。

---

## 8. 部署形态

- **开发**：后端 uvicorn `:8000` + 前端 Vite `:5173`，前端默认连本机 `:8000`（跨源由 CORS 白名单放开局域网/`.local`）。
- **生产（树莓派）**：`scripts/deploy/setup.sh` 构建 `frontend/dist` → 建 venv 装锁版本依赖 → 写**唯一** systemd 服务 `ec-backend`（页面+API+WS 同源 `:8000`）→ 配置 Chromium Kiosk 自启（Labwc/X11 两种分支）。局域网访问用 `EC_BIND=0.0.0.0`。

---

## 9. 工程约定（合作前必读）

摘自 `CONTRIBUTING.md`，与本项目强相关的几条：

- `main` 必须可运行；分支 `feat/ fix/ docs/ test/ refactor/ chore/`；提交信息 `类型: 描述`
- **数据规则**：Raw/Calibrated/Derived 分层，原始数据不得被滤波/温补/校准覆盖；每帧必带 `seq`/`timestamp_utc`/`monotonic_ms`
- **测量链路规则**：禁止用未验证的直流长期激励；激励频率/幅值/拓扑必须有台架证据；电导率 I–V 链路与三电极恒电位仪**分开验收**
- **PR 必须说明**：目的、改动、验证命令与结果、对接口/数据/测量/硬件的影响、风险与回退、截图或台架证据
- **禁止**：提交密钥/venv/原始数据、静默改 Schema、用销售宣传参数替代台架验证

---

## 10. 路线图

| 阶段 | 内容 |
|---|---|
| Phase 4 | DS18B20 温度通道驱动与位置验证 |
| Phase 5A/B/C | 电气台架（激励/AFE/ADC/精密负载）→ 导电池 + Kcell 标准液标定 → G/κ(T)/κ25 闭环与温补验证 |
| Phase 6 | pH/ORP 通道（与电导率链路分离） |
| Phase 8 | 判稳阈值台架标定、PDF 报表（代表值/误差预算/残差） |
| Phase 9 | ESP32-S3 时序控制与同步采集、硬件在环 |
| Phase 10+ | 三电极电化学扩展（恒电位仪/CV），不得倒灌进电导率 MVP |

---

## 11. 文档地图：去哪看什么

| 想了解 | 看这里 |
|---|---|
| 全局技术与 Bug 细节 | `项目文档.md`（本仓库最新、最全，13 章 + 附录） |
| 本文：整体认知与叙事 | `项目概述.md` |
| 快速开始与进度真相 | 根 `README.md` |
| 接口契约（WS/REST/拟合/调试） | `docs/接口说明.md` |
| 数据格式与分层定义 | `docs/接入数据格式.md` |
| 硬件路线与验收顺序 | `docs/电导率I-V测量链路与开发路线.md` |
| 历史评审与遗留缺陷追踪 | `docs/V2-regression-postmortem.md`、`docs/已知问题.md`（注：早期评审稿 `REVIEW_round*` 未入库，引用时以其结论转录为准） |
| 已知问题与限制 | `docs/已知问题.md` |
| 协作规范 | `CONTRIBUTING.md` |
| 设备配置样例 | `configs/devices/*.json` + `configs/README.md` |
| 部署 | `scripts/deploy/README.md`、`setup.sh` |
