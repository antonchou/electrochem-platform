# 数据源模式：server = 连接后端（默认，模拟源/真实后端共用）；browser = 纯浏览器内置模拟（无需任何后端）
VITE_DATA_SOURCE=server

# server 模式下连接的后端地址。留空时自动沿用当前页面的主机名和安全协议，端口为 8000；
# 跨主机或使用反向代理时再显式填写，例如 wss://example.test/ws/stream 与 https://example.test。
VITE_WS_URL=
VITE_API_BASE=
