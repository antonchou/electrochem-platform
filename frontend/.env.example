# 数据源模式：server = 连接后端（默认，模拟源/真实后端共用）；browser = 纯浏览器内置模拟（无需任何后端）
VITE_DATA_SOURCE=server

# server 模式下连接的后端地址。留空时使用「当前主机名 + 8000 端口」：
#   - 生产/Kiosk：页面也由 FastAPI 在 :8000 托管，因此与 API/WS 同源
#   - 开发：Vite 在 :5173 提供页面，仍连 :8000 的 API/WS
# 跨主机或反向代理时再显式填写，例如 wss://example.test/ws/stream 与 https://example.test。
VITE_WS_URL=
VITE_API_BASE=
