# 项目交接文档

> 适用对象：接手「树莓派在电化学设备的应用开发 · 溶液导电性相对比较实验」Web/后端/测试的开发者。
> 最后更新：2026-08-23 ｜ 当前覆盖 **严格 V2 电极 I–V Mock、Phase 3 Web 前端、Phase 7 数据存储与导出、备选公式拟合**；物理测量链仍待台架接入与验证。

---

## 1. 项目一句话

面向树莓派的电化学溶液实验平台：**模拟数据源（FastAPI）→ 实时 Web 界面（React）→ SQLite 持久化 → 历史回看/导出 → 化学公式拟合**。当前后端为模拟设备；真实电导率不使用 BA121S/CM2 等现成模块，而是采集电极电压、电流和温度，计算 `G → κ(T) → κ25`。完整边界见 `docs/电导率I-V测量链路与开发路线.md`。

## 2. 关键位置与环境

| 项 | 位置 / 说明 |
|---|---|
| 仓库根 | 当前 Git checkout；用 `git rev-parse --show-toplevel` 获取，文档不依赖某台电脑的绝对路径 |
| 当前基线 | `main` @ `e0429ab`（严格 V2 I–V 测量链修复基线） |
| Node.js | **≥24**（`.nvmrc`=24） |
| Python | ≥3.11；推荐后端虚拟环境 `backend/.venv`（Windows 用 `.venv\Scripts\python`） |
| 数据文件 | `data/raw/ec.db`（SQLite，**不入 Git**，由 `.gitignore` 排除） |

## 3. 最快上手（模拟模式，无需硬件）

```bash
# 1) 后端（端口 8000）
cd backend
python3 -m venv .venv                       # 首次
.venv/Scripts/pip install -r requirements.txt
.venv/Scripts/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 2) 前端（端口 5173，Node 24）
cd ../frontend
npm install                                  # 首次
npm run dev                                  # 浏览器打开 http://localhost:5173
```

**演示路径**：填样品编号（如 `NACL_004`）→ 开始实验 → 查看 U/I/T、G、κ(T)、κ25、校准/质量状态与分层曲线 → 停止 → 导出 CSV + 化学公式拟合（选 X 轴/模型）→ 历史实验 → 详情回看 + 再拟合。

**测试**：
```bash
cd backend && .venv/Scripts/python -m pytest tests -q        # 87 项
cd frontend && npm run typecheck
cd tests/e2e && npm install && npx playwright test                     # 24 用例（P03 默认跳过）
```
> Windows 本机无 Playwright Chromium 时，可在 PowerShell 运行
> `$env:E2E_BROWSER='msedge'; npx playwright test`（详见 §11）。

## 4. 仓库结构全解

```
electrochem-platform/
├── backend/                # FastAPI 后端（含模拟数据源）
│   ├── app/
│   │   ├── main.py         # FastAPI 入口；lifespan 启停持久化
│   │   ├── broadcast.py    # 每客户端独立有界发送队列（慢连接隔离）
│   │   ├── drivers/        # DeviceDriver + stable/noisy/drift/dropout Mock
│   │   ├── schemas.py      # Pydantic 模型（严格 V2 帧/控制/拟合请求）
│   │   ├── measurement.py  # U/I → G 与 Kcell 单位换算
│   │   ├── calibration.py  # κ(T) / κ25、校准与质量标志
│   │   ├── state.py        # 实验状态单例（idle/running/stopped/error）
│   │   ├── stream.py       # 模拟数据生成器 generate_frame（10Hz）
│   │   ├── routes.py       # WS 实时流 + REST 控制 + 历史/导出 + 拟合 + 调试接口
│   │   ├── storage.py      # SQLite 四表 + 版本迁移 + append-only 触发器 + DAO
│   │   ├── persistence.py  # 异步落库（Queue 攒批 200 条/0.5s，不阻塞 WS）
│   │   └── analysis.py     # 化学公式拟合（纯 Python 最小二乘，无 numpy）
│   ├── tests/              # pytest（test_api/test_storage/test_analysis）
│   └── requirements.txt
├── frontend/               # React 18 + TS + Vite + ECharts（按需引入）+ CSS Modules
│   ├── src/
│   │   ├── config/config.ts        # 唯一配置入口（WS/API 地址、曲线参数）
│   │   ├── types/protocol.ts       # 协议类型 + parseServerMessage 校验（唯一事实来源）
│   │   ├── services/               # 通信层（websocketClient/apiClient/browserMock/index）
│   │   ├── hooks/                  # useConnection/useExperiment/useRealtimeData
│   │   ├── components/             # 纯 UI 组件（含 FitPanel/HistoryPanel/StaticChart）
│   │   └── pages/ExperimentPage.tsx # 主实验页（编排）
│   └── .env.example                # VITE_WS_URL / VITE_API_BASE（切真实后端）
├── firmware/               # ESP32-S3 固件（占位，后续阶段）
├── configs/                # 设备/校准/模板配置（版本化，rule 35/36）
├── data/raw|calibrated|derived    # raw=SQLite 不可变数据；后两者预留
├── tests/e2e/              # Playwright（experiment/fit/history/screenshots/stability）
├── scripts/deploy/         # 树莓派一键部署（setup.sh + systemd + Kiosk）
├── docs/                   # 接口说明.md / 已知问题.md / 项目交接文档.md / screenshots/
├── .nvmrc + .gitignore + README.md
```

## 5. 核心架构与数据流

```
                    ┌─────────────── 实时链路（10Hz）───────────────┐
 [Mock U/I/T] → G/κ(T)/κ25 → V2 ws_frame ─► WebSocket ─► websocketClient ─► 曲线/数值（200ms 刷新）
        │
        └► persistence（Queue 攒批）─► SQLite raw_frames（append-only）
                                        │
                                        ├─► REST 历史列表/详情/帧
                                        ├─► 导出 CSV / JSON
                                        └─► 拟合：POST /api/analysis/fit ─► FitPanel（结果区+历史详情复用）
```

关键设计：
- **append-only（REQ-D-001）**：`raw_frames` 建触发器禁止 `UPDATE`/`DELETE`，原始数据不可篡改，导出即取证。
- **先入持久化队列再推送**：采集循环先 `enqueue` 再 `broadcast`，停止时 `flush`，避免快速停止丢最后一帧。
- **广播背压隔离**：采集帧进入每客户端独立有界队列；慢客户端只丢自己的旧待发帧，不阻塞采集与其他客户端。
- **统一驱动边界**：`DeviceDriver` 定义异步 connect/read/close；Mock 场景可配置、可复现，真实电压/电流/温度采集按同接口接入。
- **帧溯源（rule 38）**：落库帧带 `seq_no / timestamp_utc / monotonic_ms / t_seconds / sensor_path_id / sample_id`，前端 timestamp 仅显示。
- **切真实后端零改动**：改 `frontend/.env.local`（或 `config/config.ts`）指向真实地址即可，协议见 `docs/接口说明.md`。

## 6. 已实现功能清单

### 6.1 实时界面（Phase 3，F01–F10）
启动加载 / 开始停止重开状态机 / 实时 U/I/T、G、κ(T)、κ25 与校准/质量状态 / κ(T)、κ25、U、I 主图 + 独立温度图 / WebSocket 断线 3s 提示 + 自动重连 / 严格拒绝 V1 坏帧 / 10 Hz 持续流 / 1 万点不卡（burst 验证）。缺测帧保留固定 V2 键并以 `null + quality_flags` 广播和落库。

### 6.2 数据存储与导出（Phase 7）
SQLite 四表（experiments / samples / raw_frames / calibration_records）；异步落库；历史实验列表/详情/原始帧；CSV/JSON 导出；样品溯源（sample_id / sensor_path_id / 浓度可选）。

### 6.3 化学公式拟合（备选公式）
- 按 **X 轴语义** 分模型池（`x_axis`）：
  - `time`：线性 / 二次 / **一阶指数饱和** y=a−b·e^(−k·x) / 指数 / 对数 / 幂
  - `temperature`：线性温补 / 二次 / **Arrhenius** y=a·e^(−Ea/(R·T))（输出 `Ea_kJ_mol`）
  - `concentration`：线性标定 / 二次 / **Kohlrausch** y=c·(a−b·√c)（a=Λ∞、b=K）
- 纯 Python 高斯消元解正规方程，零 numpy 依赖（树莓派友好）；非线性（一阶饱和）用 k 对数网格搜索。
- 前端 `FitPanel` 为共享组件：结果区与历史详情页复用，X 轴切换、模型多选、结果表按 R² 排序、最优高亮、曲线叠加。

> **交付范围说明**：本节所列拟合与存储为**当前交付范围**；SRS 冻结基线中的 R-1~R-6（REQ-M-001 / REQ-F-001·002 / REQ-D-003 / REQ-C-001 / REQ-U-001 / SRS 3.3 协议）属**后续阶段**，不在本节清单内，明细见 README「已知缺口与交付阶段」。

## 7. API 摘要（完整见 `docs/接口说明.md`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/health` | 健康检查 |
| POST | `/api/experiment/start` | 开始实验（body 可选 `sample_id/sensor_path_id/title/...`） |
| POST | `/api/experiment/stop` / `reset` | 停止 / 复位 |
| WS | `/ws/stream` | 实时数据帧（10Hz，含状态帧） |
| GET | `/api/experiments` | 历史列表 |
| GET | `/api/experiments/{id}` | 详情（含样品汇总） |
| GET | `/api/experiments/{id}/frames` | 原始帧（limit 参数） |
| GET | `/api/experiments/{id}/export.csv` / `.json` | 导出 |
| POST | `/api/analysis/fit` | 化学公式拟合（body: `x/y/models/x_axis`） |
| POST | `/api/debug/bad-frame` | 注入坏帧（验收 F10） |
| POST | `/api/debug/close-connections` | 强制断开 WS（验收 F08/F09） |
| POST | `/api/debug/burst?count=N` | 快速推 N 帧（验收 P04） |

## 8. 测试体系

| 套件 | 命令 | 规模 | 覆盖 |
|---|---|---|---|
| backend pytest | `cd backend && .venv/Scripts/python -m pytest tests -q` | **87 项** | 严格 V2/未校准/掉点、控制并发、驱动/背压、版本迁移、append-only、历史/导出、拟合 |
| frontend typecheck | `cd frontend && npm run typecheck` | 全量 TS | 协议、hooks、组件与 ECharts 配置类型 |
| E2E | `cd tests/e2e && npx playwright test` | **24 用例（P03 默认跳过）** | F01–F10/F03b/F04b、P02/P04/P04c、历史全流程、拟合、交付截图 |
| P03 长跑（可选） | `STABILITY_MINUTES=30 npx playwright test -g "P03"` | 可选 | 30 分钟稳定性（默认跳过） |
| Phase 2 后端长跑 | `python scripts/phase2_soak.py --duration 1800 --sample-rate 10` | 18,000 帧量级 | REST→WS→SQLite、序号/时钟/质量标志连续性 |

## 9. Git 约定

- 提交信息规范参考《仓库与工程约定》41–46，按 **chore / feat(backend|core|api|frontend|analysis|test)** 分片。
- 当前交付基线为 `main` @ `e0429ab`。最近的主线节点如下；完整历史以 `git log --oneline` 为准：

```
e0429ab fix: enforce strict V2 I-V measurement pipeline
e8421fc Merge pull request #4 from antonchou/iv-measurement-v2
196b725 feat(frontend): layered kappa(T)/kappa25 UI with raw diagnostics
ca7a464 feat(backend): V2 protocol frame and versioned SQLite migration
0d043b4 feat(backend): pure U/I/T driver with measurement/calibration chain
a0baa48 docs: revise conductivity measurement route
```

- `.gitignore` 已排除 `node_modules/.venv/dist/data/.env*/私钥`（rule 37）；`data/raw/ec.db` 不入库。

## 10. 数据与配置

- **DB 路径**：`backend/app/storage.py` 默认 `data/raw/ec.db`，可用环境变量 `EC_DB_PATH` 覆盖（测试用它隔离）。
- **前端地址**：`frontend/src/config/config.ts`；`frontend/.env.example` 示范切真实后端。
- **调试 burst 参数**：`backend/app/stream.py` 的 `generate_frame` 生成严格 V2 调试帧；每次 API 调用使用独立 `DEBUG-BURST-*` 溯源，不落库。
- **模拟驱动参数**：`configs/devices/mock.example.json`；通过 `EC_MOCK_CONFIG` 加载，或用 `EC_MOCK_SCENARIO/EC_SAMPLE_RATE_HZ/EC_MOCK_SEED` 覆盖。

## 11. 已知问题与踩坑记录（详见 `docs/已知问题.md`）

1. **Edge channel teardown 可能较慢**：`E2E_BROWSER=msedge` 下曾观察到 worker 收尾等待。优先安装 Playwright Chromium；使用系统 Edge 后应确认无测试进程残留。
2. **Edge 下 `<a download>` 不触发 Playwright download 事件**：会被 Edge 下载中心接管（导航到 `edge://downloads-hub/`）。E2E 改用 `page.request.get` 断言 CSV 内容。
3. **P03 30 分钟长跑**：默认跳过，需 `STABILITY_MINUTES` 显式指定。
4. **物理设备驱动未接入**：Mock 已完整实现 V2 U/I/T → G/κ(T)/κ25 软件闭环；真实 AFE/ADC/温度通道仍需按同一 Driver 与协议接入并做台架验证。

## 12. 树莓派部署

```bash
cd scripts/deploy && chmod +x setup.sh && ./setup.sh
```
脚本自动完成：构建前端 → 装 backend 依赖 → 生成 systemd 服务（`ec-backend` 8000 / `ec-web` 5173）→ Chromium Kiosk 开机全屏。详见 `scripts/deploy/README.md`。

## 13. 后续路线图（按垂直切片 rule 40）

> 未实现项参见 **README「已知缺口与交付阶段」**：R-1~R-6（REQ-M-001 / REQ-F-001·002 / REQ-D-003 / REQ-C-001 / REQ-U-001 / SRS 3.3）属冻结 SRS 但当前未实现，**勿误判为已满足冻结 REQ**。下列路线图条目即对应这些缺口的交付载体。

1. **真实设备驱动接入**：让硬件驱动输出现有 `DriverReading` 的 U/I/T，复用当前计算、质量、持久化和 V2 WS 链；新增真实 `device_id/firmware_version`。在线端不回退兼容 V1 帧。
2. **电气测量台架**：完成受控交流/双极性激励、电压通道、电流通道、ADC 与保护；用精密阻抗验证增益、零点、波形算法、量程和异常检测。
3. **导电池校准/温补**：用标准液和真实硬件验证现有 `G → κ(T) → κ25` 计算链；原始 U/I/T 不可变，并关联 calibration_id、标准液批次和温补模型版本。随后实现判稳与 QC。
4. **拟合入库与报表**：Kohlrausch/线性标定结果写 `data/derived/`，PDF 报表导出。→ 补齐 CI/残差/RMSE/MAE/AICc/留一交叉验证与有效浓度区间、禁止外推约束（REQ-F-001/002）。
5. **固件**：ESP32-S3 负责激励时序、同步采集和质量检查（`firmware/` 占位）。三电极恒电位仪属于后续扩展，不与电导率 MVP 混合。

## 14. 接手第一步 Checklist

- [ ] `git config user.name/email` 改成你的身份
- [ ] 按 §3 启动前后端，浏览器打开 5173 走一遍演示路径
- [ ] 跑一遍 pytest（87 项）、前端 typecheck 与 E2E（24 用例，P03 默认跳过），确认环境 OK
- [ ] 读 `docs/接口说明.md`（协议）与 `docs/已知问题.md`（坑）
- [ ] 确认下一个里程碑目标（见 §13），在 `git log` 基础上继续分片提交
